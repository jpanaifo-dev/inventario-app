// Entidad principal del inventario
// Aquí defines las propiedades y métodos del inventario

export interface Inventario {
  id: string;
  nombre: string;
  cantidad: number;
  ubicacion: string;
  // ...otros campos relevantes
}
